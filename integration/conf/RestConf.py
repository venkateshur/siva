class RestConf(object):
    def __init__(self, api,  cert_directory, cert_name, cert_password):
        self.api = api
        self.cert_directory = cert_directory
        self.cert_name = cert_name
        self.cert_password = cert_password
