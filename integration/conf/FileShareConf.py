class FileShareConf(object):
    def __init__(self, system_type, host, port, user_name, password, path):
        self.system_type = system_type
        self.host = host
        self.port = port
        self.user_name = user_name
        self.password = password
        self.path = path
